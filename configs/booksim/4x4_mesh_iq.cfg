// Topology
topology = mesh;
k = 4;
n = 2;

// Routing
routing_function = dim_order;

// Flow-control is wormhole by default

// Flow control
router = iq;
num_vcs = 1;
vc_buf_size = 8;

// Delays (Injection + LT + [RC + VA + SA + ST] + LT)
credit_delay = 1;
routing_delay = 1;
vc_alloc_delay = 1;
sw_alloc_delay = 1;
st_prepare_delay = 0;
st_final_delay = 1;
noq = 0;

// Traffic
classes = 1;
